var nav = require('./nav').init();
var contact = require('./contact').init();
var map = require('./map').init();

//var glitcher = require('./glitcher');
//glitcher.init(document.getElementById('canvas'));
